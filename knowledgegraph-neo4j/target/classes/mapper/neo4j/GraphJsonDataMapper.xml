<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.knowledgegraph.neo4j.mapper.GraphJsonDataMapper">

    <select id="getTest" resultType="map">
        ${cqlString}
    </select>

    <select id="getTestLink" resultType="map">
        MATCH (n:JavaAtlas)-[r]->(b)  return id(r) as id, r.name as name , id(n) as from ,n.name as fromName, id(b) as to , b.name as toName
    </select>

    <select id="getTestNode" resultType="map">
        MATCH (n:JavaAtlas) return n.name as name , id(n) AS id
    </select>



</mapper>
